#!/usr/bin/env node

/**
 * =======================================================================
 * NO1R LEG4CY - MACRO TEST SUITE SETTINGS
 * Use this file to test all available shortcodes/macros.
 * Make sure to replace the SMTP credentials with your own.
 * =======================================================================
 */

const smtp = [
  {
    "host": "smtp.gmail.com",
    "port": 587,
    "secure": false,
    "username": "nfdconstructionsllc@gmail.com",
    "password": "", // MASUKKAN APP PASSWORD GMAIL DI SINI
    "from_email": "nfdconstructionsllc@gmail.com"
  }
];

const setup = {
  key: "put your key ",
  MultipleSmtp: "SINGLE", // ROTATE or SINGLE
  mail_priority: "HIGH", // HIGH, NORMAL, LOW
  sleep_time: 5, // Delay in seconds between sending attempts
  sleep_after_count: 5, // Pause the script after sending this many emails
  isMultiThread: false, // Enable multi-threading for faster delivery
  howManyThread: 1, // Number of simultaneous worker threads to spawn

  // --- CONTENT DECEPTION & RENDERING ---
  LetterToImage: true, // Convert HTML email body into an image
  fileName_LetterToImage: "./letterToImage.html", // HTML source file for image rendering
  width_imageLetter: 800, // Resolution width of the rendered browser window
  height_imageLetter: 1000, // Resolution height of the rendered browser window
  width_imageLetter_2: "100%", // Display width of the rendered image in the email
  height_imageLetter_2: "auto", // Display height of the rendered image in the email
  
  // =====================================================================
  // TEST: MULTIPLE IMAGE ROTATION
  // Script will randomly pick one of these files to render as an image.
  // Ensure you have created these files in your root directory!
  // =====================================================================
  use_multiple_images: true, // Set to TRUE for testing
  multi_images: [
    "./letterToImage1.html",
    "./letterToImage2.html",
    "./letterToImage3.html"
  ],

  // =====================================================================
  // TEST: MULTIPLE LETTER ROTATION
  // Script will randomly pick one of these HTML files as the email body.
  // Ensure you have created these files in your root directory!
  // =====================================================================
  use_multiple_letters: true, // Set to TRUE for testing
  letter: "letter.html", // Default fallback if multiple is off
  multi_letters: [
    "./letter1.html",
    "./letter2.html",
    "./letter3.html"
  ],
  
  // =====================================================================
  // MACRO TEST: SENDER & SUBJECT ROTATION
  // =====================================================================
  use_noreply: false, // Override sender to show a randomized "noreply@domain" address
  
  // Test Basic Sender Name
  from_name: "Test <RANDFULLNAME> - <COMPANYNAME>",
  
  // Test Sender & Reply-To Address
  from_mail: "contact-<LOWER_RANDSTRING5>@<DOMAIN>", 
  replyTo: "support-<RANDNUM4>@<DOMAIN>", 
  
  // Test Basic Subject
  mail_subject: "Test <DATE> | Hello <NAME> from <COMPANYNAME>",

  // Test Multi From Name 
  multiple_fromName: true,
  multi_FromName: [
    "System <CAPITAL_RANDSTRING4>",
    "<RANDFNAME> from Support",
    "<AUTOFROMMAIL> Administrator"
  ],
  
  // Test Multi Subject 
  multiple_subject: true,
  multi_subject: [
    "Test 1: Hello <NAME>, Your email is <EMAIL>",
    "Test 2: Current time is <TIME> - ID: <RANDNUM10>",
    "Test 3: Domain <DOMAIN> | Hash: <RANDSTRING8>"
  ],

  multiple_links: [
    "https://test-link-1.com/?user=<EMAIL>",
    "https://test-link-2.com/?id=<RANDNUM5>"
  ],

  test_frommail: false, // Enable testing mode for the frommail list
  frommail_filename: "frommail.txt", // File containing custom "From" addresses
  hide_frommail: false, // Masks the actual sender address using header manipulation
  
  email_test: true, // Send a copy to the test destination every X emails
  test_every: 1, // Frequency of test emails
  email_destination: "asd@asd.com", // REPLACE WITH YOUR TEST EMAIL
  mailist: "mailist.txt", // The file containing your target recipient list
  
  // --- ASSETS & BRANDING ---
  useAttachment: true, // Enable to test attachments
  attachmentName: "Doc_<COMPANYNAME>_<DATE>", // Macro in default Attachment Name
  useGrabLogo: true, // Automatically fetch the logo of the recipient's company
  logoWidth: "450px", // CSS width for the grabbed logo
  logoMaxWidth: "500px", // CSS max-width for the grabbed logo
  logoPadding: "10px", // CSS padding around the logo
  defaultLogo: "https://img-prod-cms-rt-microsoft-com.akamaized.net/cms/api/am/imageFileData/RE1Mu3b?ver=5c31", // Fallback logo URL
  useMylogo: true, // Use your custom fixed logo from the myLOGO section
  
  widthImage_for_myLogo_letter: "150",
  heightImage_for_myLogo_letter: "50",
  widthImage_for_grabLogo_attachment: "200",
  heighttImage_for_grabLogo_attachment: "auto",

  proxy: "" // SOCKS5 proxy URL (e.g., socks5://user:pass@host:port)
};

// =====================================================================
// EMAIL VALIDATOR & CLEANER (STANDALONE & ON-THE-FLY)
// =====================================================================
const email_validator = {
  // SET TO FALSE SO IT SENDS EMAIL AND TESTS THE TEMPLATES!
  run_standalone: false,         
  active: true,                  // TRUE = Filter bad emails during normal sending process.
  check_mx: false,               // TRUE = Check active DNS/MX Record (Warning: Slow for lists > 10,000).
  filter_disposable: true,       // TRUE = Automatically skip disposable/temporary emails (e.g., 10-minute mail).
  save_rejected: true,           // TRUE = Save rejected/bad emails to a separate file.
  rejected_file: "bad_emails.txt",// Filename for rejected emails.
  input_file: "mailist.txt",     // Source file containing the raw email list (Standalone mode only).
  output_folder: "./Filtered_Emails" // Folder to save the sorted results (Standalone mode only).
};

// =====================================================================
// MACRO TEST: ATTACHMENT NAMES
// =====================================================================
const attachment = [
  {
    filename: "Invoice_<COMPANYNAME>_<RANDNUM5>.pdf",
    path: "attachment.html",
    attachmentToPdf: true, // Automatically convert HTML source to a PDF file
    encryptLevel: 10, // Complexity level of content obfuscation
    pdfWidth: "800px", // Custom width of the generated PDF page
    pdfHeight: "1100px", // Custom height of the generated PDF page
    injectBinaryNoise: true // Modifies file hash on every send to prevent signature detection
  }
];

// =====================================================================
// MACRO TEST: EML BUILDER
// =====================================================================
const attachment_EML = {
  use_EML: true, // Enable to test EML
  attachment_name_EML: "Message_from_<COMPANYNAME>_<DATE>.eml", // Filename for the EML attachment
  subject_eml: "Internal Note: <EMAIL> - <TIME>", // Internal subject of the EML
  from_email_eml: "<RANDFULLNAME> Support", // Internal sender name of the EML
  letter_eml: "EML/letter.html", // Source HTML for the EML body
  useAttachment_EML: true, // Enable extra attachments inside the EML itself
  attachment: [
    { 
      attachmentName: "<COMPANYNAME>_Encrypted_<RANDNUM4>.html", 
      path: "EML/attachment.html" 
    }
  ]
};

// =====================================================================
// MACRO TEST: CALENDAR INVITE
// =====================================================================
const calendar_invite = {
  active: false, // Set to true to test Macros in calendar invites
  TIMEZONE: "America/Adak", // Timezone for the event
  SUMMARY: "Meeting with <NAME> (<COMPANYNAME>)", // Calendar event title
  DESCRIPTION: "Hello <RANDFNAME>, here is your code: <CAPITAL_RANDSTRING6>", // Event body text
  LOCATION: "<DOMAIN> HQ", // Event location field
  METHOD: "REQUEST", // ICS method (REQUEST, PUBLISH, etc.)
  STATUS: "CONFIRMED", // Event status (CONFIRMED, TENTATIVE)
  ORGANIZER_CN: "<RANDFULLNAME>", // Name of the event organizer
  ORGANIZER_EMAIL: "admin@<DOMAIN>", // Email of the event organizer
  START_DATE: "01/07/2026", // Start date in DD/MM/YYYY format
  START_TIME: "10:30", // Start time in 24-hour format
  DURATION_MINUTES: 15 // Duration of the event
};

const generate_QRCODE = {
  activate: true, // Generate a unique QR code in the email
  link: "https://google.com?q=<EMAIL>&id=<RANDNUM5>", // Test macro in QR URL
  width: "200", // Image width in pixels
  height: "200", // Image height in pixels
  foreground_color: "#000000",
  background_color: "#ffffff"
};

const generate_random_number = [
  { name: "RANDNUM1", length_value: 1 }, // Macro for a 1-digit random number
  { name: "RANDNUM4", length_value: 4 }, // Macro for a 4-digit random number
  { name: "RANDNUM5", length_value: 5 }  // Macro for a 5-digit random number
];

const generate_random_string = [
  { name: "RANDSTRING1", length_value: 5, type: "UPPERCASE" } // Macro for a 5-letter uppercase string
];

const setup_time_date = {
  time_zone: "America/Adak", // Global timezone for date macros
  time_format_lang: "en" // Language for date/time formatting
};

const intelligence_suite = {
  run_smtp_scanner: false, // Enable SMTP scanner
  smtp_source_file: "smtpm.json", // File containing the list of SMTPs to scan
  smtp_result_file: "activesmtp.json", // File to save working SMTPs
  save_working_only: true, // Only save accounts that successfully reach the inbox
  bounce_wait_time: 10, // Delay to wait for bounce notifications (seconds)
  test_to: "asd@asd.com" // Target email for inbox verification
};

const open_redirect_scanner = { 
  active: false, // Enable open redirect scanner
  url_targets: "urls_to_scan.txt", // File containing target URLs
  redirect_payload: "https://www.bing.com", // Payload to test redirection
  output_file: "vulnerable_redirects.txt", // File to save vulnerable URLs
  redirect_timeout: 5000 // Timeout for each scan in milliseconds
};

const others = {
  change_ip: false, // Enable manual or automatic IP rotation module
  letterImageName: "tve.png", // Filename used for the LetterToImage screenshot
  cid: "JOKDIGLAFGAE", // Custom Content-ID prefix for inline images
  rejectUnauthorized: false, // Ignore SSL certificate errors (useful for some proxies/SMTPs)
  att_CT: "text/html" // Default Content-Type for attachments
};

const myLOGO = [{ function_name: "MYLOGO", nameFile: "https://drive.google.com/uc?id=1oMBVsQBm4ooCDJHc6JoIk3MNpsufMbQN" }]; // Configuration for custom brand logo

module.exports = {
  smtp, 
  setup, 
  generate_random_number, 
  generate_random_string, 
  setup_time_date, 
  attachment, 
  myLOGO, 
  attachment_EML, 
  others, 
  generate_QRCODE, 
  calendar_invite,
  redirect_link: { useRedirect: false, key: "tes1", enc_email: "c" }, 
  intelligence_suite,
  open_redirect_scanner,
  email_validator
};
